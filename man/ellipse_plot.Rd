% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ellipse.R
\name{ellipse_plot}
\alias{ellipse_plot}
\title{Plot Grouped Data with Elliptical Contours}
\usage{
ellipse_plot(
  data,
  x,
  y,
  groups = NULL,
  colors = NULL,
  levels = c(0.2, 0.5, 0.7, 0.9),
  outer_level = 0.95,
  fill = "grey60",
  alpha = 0.15
)
}
\arguments{
\item{data}{A data frame containing the variables to be plotted.}

\item{x, y}{<\code{\link[rlang:args_data_masking]{data-masking}}> Unquoted names of the
numeric variables to be plotted on the horizontal and vertical axes.}

\item{groups}{Optional <\code{\link[rlang:args_data_masking]{data-masking}}> unquoted
name of a grouping variable. If supplied, points are colored by group and
ellipses are computed separately for each group. If omitted, the ellipses
are computed from all observations.}

\item{colors}{Optional vector of colors passed to
\code{\link[ggplot2:scale_color_manual]{ggplot2::scale_color_manual()}}. The vector may be named to associate
colors explicitly with group levels. If \code{NULL}, the default ggplot2 color
scale is used.}

\item{levels}{Numeric vector containing the confidence levels of the inner
ellipse contours. Values must lie between zero and one. The default is
\code{c(0.2, 0.5, 0.7, 0.9)}.}

\item{outer_level}{Numeric value giving the confidence level of the outer,
filled ellipse. The default is \code{0.95}.}

\item{fill}{Color used to fill the outer ellipse. The default is \code{"grey60"}.}

\item{alpha}{Numeric value between zero and one controlling the transparency
of the filled outer ellipse. The default is \code{0.15}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot}} object. Additional ggplot2 layers and theme
modifications can be added to the returned object.
}
\description{
Creates a scatter plot of two numeric variables and adds elliptical contours
separately for each group. Several inner contours are drawn as black lines,
while an outer contour is displayed as a semi-transparent polygon.
}
\details{
Ellipses are calculated by \code{\link[ggplot2:stat_ellipse]{ggplot2::stat_ellipse()}} using its default
distributional assumptions. The ellipses describe the estimated bivariate
distribution within each group; they are not confidence regions for the
group means.

The ellipse levels supplied through \code{levels} are drawn in black and are not
included in the legend. The outer ellipse uses the same grouping structure
as the points, but has a common fill color and is also excluded from the
legend.

A group must contain enough non-collinear observations for its covariance
matrix and ellipse to be estimated. Groups with too few observations or a
singular covariance matrix may produce warnings and no ellipse.
}
\examples{
## Without groups
ellipse_plot(iris, Sepal.Length, Sepal.Width)


## With groups
ellipse_plot(
  iris,
  Sepal.Length,
  Sepal.Width,
  groups = Species
)

}
\seealso{
\code{\link[ggplot2:stat_ellipse]{ggplot2::stat_ellipse()}}, \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}
}
